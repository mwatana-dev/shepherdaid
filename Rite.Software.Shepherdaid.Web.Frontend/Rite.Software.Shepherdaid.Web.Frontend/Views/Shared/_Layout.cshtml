<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Security</title>
    <link rel="stylesheet" type="text/css" href="http://cdn.datatables.net/1.10.9/css/jquery.dataTables.min.css">


    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>

    <div class="container">
        <div class="navbar-light bg-white mt-0 fixed-top">
            <h2 class="text-center">[NAME OF INSTITUTION]</h2>
        </div>
    </div>
    <div class="container-fluid">

        <div class="navbar navbar-expand-sm navbar-light bg-light mt-5 fixed-top my-navbar">
            <div class="navbar offset-md-2 col-md-8">
                <a href="#" class="navbar-brand">Shepherd Aid</a>
                <button class="navbar-toggler" data-toggle="collapse" data-target="#collape-menu">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="collape-menu">
                    <ul class="navbar nav">
                        @*@if (User.IsInRole(Rite.Software.Shepherdaid.Roles.MySystemRole.ManageChurch))
                        {
                            <li>@Html.ActionLink("Manage Churches", "Index", "Churches")</li>
                        }
                        @if (User.IsInRole(MySystemRole.ManageDiocese))
                        {
                            <li>@Html.ActionLink("Manage Dioceses", "Index", "Diocese")</li>
                        }
                        @if (User.IsInRole(MySystemRole.ManageParishes))
                        {
                            <li>@Html.ActionLink("Manage Parishes", "Index", "Parishes")</li>
                        }
                        @if (User.IsInRole(MySystemRole.ManageGroups))
                        {
                            <li>@Html.ActionLink("Manage Groups", "Index", "ApplicationGroups")</li>
                        }
                        @if (User.IsInRole(MySystemRole.ManageRoles))
                        {
                            <li>@Html.ActionLink("Manage Roles", "Index", "AppRoles")</li>
                        }
                        @if (User.IsInRole(MySystemRole.ManageUsers))
                        {
                            <li>@Html.ActionLink("Manage Users", "Index", "AppUsers")</li>
                        }
                        @if (User.IsInRole(MySystemRole.SuperAdmin))
                        {
                            <li>@Html.ActionLink("Enable Users", "Disabled", "AppUsers")</li>
                        }*@
                        <li class="nav-item">
                            <a href="#" class="nav-link">Members</a>
                        </li>

                        <li class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" data-target="#setup-dropdown">
                                Setup
                            </a>
                            <div class="dropdown-menu" aria-labelledby="setup-dropdown">
                                <ul>
                                    <li>@Html.ActionLink("Manage Groups", "Index", "ApplicationGroups")</li>
                                </ul>
                                @if (User.IsInRole(Rite.Software.Shepherdaid.Roles.MySystemRole.ManageChurch))
                                {
                                    <a href="~/Views/Churches/Index.cshtml" class="dropdown-item">Manage Churches</a>
                                }
                                @if (true)
                                {
                                    <a class="dropdown-item">Manage Dioceses</a>
                                }
                                @if (true)
                                {
                                    <a class="dropdown-item">Manage Parishes</a>
                                }
                            </div>
                        </li>

                        <li class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" data-target="#security-dropdown">
                                Security
                            </a>
                            <div class="dropdown-menu" aria-labelledby="setup-dropdown">
                                <ul>
                                    <li>@Html.ActionLink("Manage Groups", "Index", "ApplicationGroups")</li>
                                    <li>@Html.ActionLink("Manage Roles", "Index", "AppRoles")</li>
                                    <li>@Html.ActionLink("Manage Users", "Index", "AppUsers")</li>
                                    <li>@Html.ActionLink("Enable Users", "Disabled", "AppUsers")</li>
                                    </ul>
                            </div>
                        </li>
                    </ul>

                </div>
            </div>
        </div>

    </div>

    <div class="container body-content">

        @RenderBody()
        <hr />
        <footer class="text-center">
            <p>&copy; 2012 - @DateTime.Now.Year - Shepherd Aid</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)

    <script type="text/javascript" charset="utf8" src="http://ajax.aspnetcdn.com/ajax/jQuery/jquery-1.8.2.min.js"></script>
    <script type="text/javascript" charset="utf8" src="http://cdn.datatables.net/1.10.9/js/jquery.dataTables.min.js"></script>
</body>
</html>
